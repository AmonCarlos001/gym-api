server:
  port: 8080

spring:
  application:
    name: spring-boot-essentials-jpa
  datasource:
    url: jdbc:mysql://localhost:3306/gym?createDatabaseIfNotExist=true
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:Am@n2006}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
